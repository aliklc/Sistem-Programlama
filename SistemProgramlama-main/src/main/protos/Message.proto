syntax = "proto3";

package info;

option java_package = "comm.struct.info";
option java_outer_classname = "MessageProto";

message Message {
  Demand demand = 1;
  Response response = 2;
}

enum Demand {
  CPCTY = 0;
  STRT = 1;
}

enum Response {
  YEP = 0;
  NOP = 1;
}
